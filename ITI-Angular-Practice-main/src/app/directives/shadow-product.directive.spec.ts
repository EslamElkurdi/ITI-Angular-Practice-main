import { ShadowProductDirective } from './shadow-product.directive';

describe('ShadowProductDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowProductDirective();
    expect(directive).toBeTruthy();
  });
});
